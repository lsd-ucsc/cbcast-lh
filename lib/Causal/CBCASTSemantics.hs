module Causal.CBCASTSemantics where

-- define an execution type (start with tuple)
--
-- define a function :: execution -> causal-delivery?
--
-- define a function :: execution -> event -> execution
--
-- try putting a refinement type (input execution satisfies causal-delivery,
-- output execution satisfies causal delivery)
--
-- try writing down an external proof  (input execution satisfies
-- causal-delivery, output execution satisfies causal delivery)

